var a = 0
var b = 'hello world '
var c = 69

var testBool = false
print(testBool + '\n')

testBool = true
print(testBool + '\n')

if (a == 1) {
	print('a (Actual Value ' + a + ') is greater than 1\n')
}

if (a <= 1) {
	print('a (Actual Value ' + a + ') is less than or equal to 1\n')
}

if(c >= 60) {
	print('c (Actual Value ' + c + ') is greater than or equal to 60\n')
}

if (c >= 69) {
	print('c (Actual Value ' + c + ') is greater than or equal to 69\n')
}

if (a != 69) {
	print('a (Actual Value ' + a + ') is not equal to 69\n')
}

while (a < 10) {
	a = a + 1
	print(b + a + '\n')
}

a + 1 - 1